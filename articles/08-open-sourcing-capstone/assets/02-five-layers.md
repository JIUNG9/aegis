| # | Layer | Status | What it does |
|---|-------|--------|--------------|
| 0 | **Safety Foundation** | Built, shipped | The wrapper every other layer inherits. PII redaction proxy, IAM policy templates, kill switch + `aegis panic` CLI, local LLM router for regulated data, OTel GenAI audit traces, honey tokens, MCP read/write/blocked tool scoping, demo mode. Safety as features, not as a checklist. |
| 1 | **LLM Wiki Engine** | Built, shipped | Karpathy-pattern knowledge base. Reads Confluence, GitHub, SigNoz, Slack. Synthesizes one canonical markdown page per entity. Renders to `aegis-wiki` repo. Never goes stale because it never stops updating. |
| 2 | **SigNoz Connector** | Built, shipped | Pulls ClickHouse-backed traces, metrics, logs, and alert history from SigNoz. Time-pattern analyzer detects "80% on Monday 9 AM" style clusters. Feeds Layer 1 with observability data and Layer 3 with live telemetry. |
| 3 | **Claude Control Tower** | Built, shipped | The investigation orchestrator. Three modes — Eco (Haiku) / Standard (Sonnet) / Deep (Opus + pattern analyzer). Routes prompts through the local LLM router, wraps every call in OTel spans, proposes actions through the Layer 4 ladder. |
| 4 | **Production Guardrails** | Built, shipped | The 4-stage automation ladder: SUGGEST → DRAFT → PROPOSE → EXECUTE. YAML-driven policy engine. Slack / GitHub / CLI approval gates. Append-only audit log. IAM mutations capped at PROPOSE; destructive prod actions never auto-execute. |
| 5 | **MCP Document Reconciliation** | Built, shipped | Four read-scoped MCP tools — `find_docs`, `reconcile_docs`, `detect_stale_docs`, `check_doc_links`. Cross-source contradiction detection across Confluence, GitHub, Obsidian, Slack. Keeps the Layer 1 wiki honest over time. |
